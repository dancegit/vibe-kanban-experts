read README.md
do a tree 
and a git ls-files
check latest git commit
use the meta-expert to analyze all expert domains in paralell and then create agent skills in parallell, 
then make the experts self-imporove in parallell
then make a .sh bash script to build the product so i can run it with npx locally, (when i run "npx claude-flow@alpha" it should point to my local installation/build)
make a .md file in ./userguides/ explaining how the claude-flow is used as an agent and what prerequisites there are to run it/use it. 
